# --- corpus data: never commit TIMIT (LDC93S1 is licensed, not redistributable) ---
*.WAV
*.wav
*.PHN
*.WRD
*.SPH
*.sph
timit/
TIMIT/
data/

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.python-version

# --- jupyter ---
.ipynb_checkpoints/
*-checkpoint.ipynb

# --- caches / artifacts ---
.cache/
.pytest_cache/
.mypy_cache/
figs/
*.png
*.pdf

# --- exceptions: these two figures contain no TIMIT audio/text, just WER numbers ---
!wer_vs_offset_timestamps_on.png
!wer_vs_offset_timestamps_off.png
!transcribe_wer_vs_offset_timestamps_on.png
!transcribe_wer_vs_offset_timestamps_off.png

# --- offset experiment outputs (contain TIMIT reference transcripts: licensed) ---
offset_manifest.json
offset_results.csv
transcribe_results.csv

# --- frozen corpus copy (TIMIT audio + transcripts: LDC93S1, not redistributable) ---
corpus_frozen/

# --- macOS / editors ---
.DS_Store
.vscode/
.idea/
